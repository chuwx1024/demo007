import 'element-ui/lib/theme-chalk/index.css'
import Vue from 'vue'
import {
  Button,
  Select,
  Container,
  Form,
  Input,
  Row
} from 'element-ui'

Vue.use(Button)
  .use(Select)
  .use(Container)
  .use(Form)
  .use(Input)
  .use(Row)
